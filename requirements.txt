Django==6.0.2
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2025.3
pypdfium2==5.5.0

# Producción (Render)
gunicorn
whitenoise
dj-database-url
psycopg[binary]

# Media (Cloudinary)
django-cloudinary-storage

# UI (Dashboard)
django-adminlte3

# Auth (login moderno + social + hardening)
django-allauth
django-allauth-ui
slippers
django-widget-tweaks
django-axes
requests
PyJWT
cryptography; sys_platform != 'win32'
